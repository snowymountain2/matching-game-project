import React from 'react';

const singleCard = {
    backgroundColor: 'red',
    margin: '5px 5px 5px 5px',
    height: '50px',
    width: '50px'
};

const Card = () => {
    return (
        <div style={singleCard} className="single-card">

        </div>
    );
};

export default Card;